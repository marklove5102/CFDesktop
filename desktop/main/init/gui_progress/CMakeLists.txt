# gui_progress module
# Note: UI components (InitShowWidget, qrc_src) have been migrated to desktop/ui/widget/init_session/
# This directory now only contains the initialization stage logic (GuiLogoBootStage)
