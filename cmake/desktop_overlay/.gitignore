# Ignore user-customized overlay files
# Only directory structure (.gitkeep placeholders) is tracked
# User can place custom early.ini, configs, wallpapers, etc. without git noise
**

!.gitignore
!**/
!**/.gitkeep
